# Railway build configuration for Tide monorepo
# This file should be at the root of the repository

[build]
builder = "NIXPACKS"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
