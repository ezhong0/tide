[phases.setup]
nixPkgs = ["nodejs-18_x", "pnpm"]

[phases.install]
cmds = ["cd ../../.. && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd ../../.. && pnpm --filter @tide/gateway build"]

[start]
cmd = "cd packages/services/gateway && node dist/index.js"
