[phases.setup]
nixPkgs = ["nodejs-18_x", "pnpm"]

[phases.install]
cmds = ["cd ../../.. && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd ../../.. && pnpm --filter @tide/workflow-service build"]

[start]
cmd = "cd packages/services/workflow && node dist/index.js"
