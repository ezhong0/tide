{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm -r --filter './packages/shared/**' --filter './packages/libraries/**' --filter './packages/services/shared/**' build && pnpm --filter @tide/email-service build"
  },
  "deploy": {
    "startCommand": "node packages/services/email/dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
