[phases.setup]
nixPkgs = ["nodejs-18_x", "pnpm"]

[phases.install]
cmds = ["cd ../../.. && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd ../../.. && pnpm --filter @tide/email-service build"]

[start]
cmd = "cd packages/services/email && node dist/index.js"
