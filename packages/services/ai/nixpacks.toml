[phases.setup]
nixPkgs = ["nodejs-18_x", "pnpm"]

[phases.install]
cmds = ["cd ../../.. && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd ../../.. && pnpm --filter @tide/ai-service build"]

[start]
cmd = "cd packages/services/ai && node dist/index.js"
