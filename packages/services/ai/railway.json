{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf packages/services/ai/dist packages/services/ai/tsconfig.tsbuildinfo && pnpm install --frozen-lockfile && pnpm -r --filter './packages/shared/**' --filter './packages/libraries/**' build && cd packages/services/ai && pnpm exec tsc --build --force && cd ../.."
  },
  "deploy": {
    "startCommand": "node packages/services/ai/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
