{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf packages/services/ai/dist && pnpm install --frozen-lockfile && pnpm -r --filter './packages/shared/**' --filter './packages/libraries/**' build && pnpm --filter @tide/ai-service build"
  },
  "deploy": {
    "startCommand": "node packages/services/ai/dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
